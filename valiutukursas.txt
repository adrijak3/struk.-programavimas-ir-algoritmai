#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    string V[100]; int pasirinkimas;
    int P[10];
    int n, i, pazymys; string mokinys;
    cout << "Meniu - " << endl;
    cout << "1 - Peržiūrėti visų mokinių pažymius" << endl;
    cout << "2 - Konkretaus mokinio pažymys" << endl;
    cout << "3 - Atnaujinti pažymį konkrečiam mokiniui" << endl;
    cout << "4 - Mokinio šalinimas iš sąrašo" << endl;
    cin >> pasirinkimas;

    switch (pasirinkimas)
        {
        case 1:
        for (int = 0; i < n; i++)
        {
            cout << V[i] << endl;
        }
        break;
        case 2:
            cout << "Kokio mokinio ieškote? " << endl;
            cin >> n;
            for (int = 0; i < n; i++)
                {
                if (V[i] != n) cout << "Tokio mokinio nėra" << endl;
                else cout << "Mokinio pažymys -" << i << endl;
                }

            break;
            case 3:
            cout << "Kokio mokinio ieškote? " << endl; string mok;
            cin >> mok;
            cout << "Į kokį pažymį norite pakeisti? " << endl;
            cin >> pazymys;
            for (int i = 0; i < n; i++) {
                if (V[i] = mok) P[n] == pazymys;
                else cout << " Tokio mokinio nėra" << endl;
            }
            break;
        case 4:
            cout << "Kurį mokinį norite pašalinti iš sąrašo?" << endl;
            cin >> mokinys;
            for (int i = 0; i < mokinys; i++)
            {
              cout << "nera" << endl
            }
            break;
        default: cout << "Klaida" << endl;
        }

    return 0;
}